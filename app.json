{
  "name": "xokul",
  "description": "Xokul is an API application that exposes our different kinds of external services as JSON.",
  "keywords": [
    "dokku",
    "rails"
  ],
  "scripts": {
    "dokku": {
      "postdeploy": "bundle install -j4 --path /app/vendor/bundle --without development:test && bundle exec rails assets:precompile && bundle exec rails db:migrate"
    }
  }
}
